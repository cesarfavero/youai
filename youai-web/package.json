{
  "name": "youai-web",
  "version": "0.1.0",
  "private": true,
  "description": "YouAI web chat — credit dashboard and SSE streaming",
  "scripts": {
    "dev": "python3 -m http.server 3000 --directory public",
    "build": "echo 'Static chat UI in public/ — served by youai-coordinator or npm run dev'",
    "lint": "echo 'No lint configured for static MVP chat'"
  },
  "engines": {
    "node": ">=20"
  }
}